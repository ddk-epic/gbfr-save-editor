import { keyTable } from "../../core/keys";
import { SKILLBOARD_EFFECT_KEYS } from "../../data/master-traits";

export const SKILLBOARD_EFFECTS = keyTable(
  "skillboard_effect",
  SKILLBOARD_EFFECT_KEYS,
);
